# TCP 프록시 서버 설정 파일
# Key=Value 형식으로 작성

# 리스닝 포트
listen_port=9999

# 대상 서버 주소
target_host=127.0.0.1

# 대상 서버 포트
target_port=8080

# 로깅 활성화 (true/false)
enable_logging=true

# 로그 파일 경로
log_file=logs/proxy.log

# 필터 활성화 (true/false)
enable_filters=false
