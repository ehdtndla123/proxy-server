# DB 프록시 설정 파일
# MySQL/PostgreSQL 등의 DB 연결을 프록시

# 리스닝 포트
listen_port=10000

# 대상 DB 주소
target_host=127.0.0.1

# 대상 DB 포트 (MySQL: 3306, PostgreSQL: 5432)
target_port=3306

# 로깅 활성화
enable_logging=true

# 로그 파일 경로
log_file=logs/db_proxy.log

# 필터 비활성화 (DB는 안정적인 연결 필요)
enable_filters=false
